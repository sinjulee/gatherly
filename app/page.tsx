import { Camera, Clock3, Mic, MoreHorizontal, Play } from "lucide-react";
import { AddButton, PageHeader } from "@/components/app-shell";

const stats = [{ label: "오늘 모은 자료", value: "12", unit: "개", tone: "bg-mint" }, { label: "정리할 메모", value: "4", unit: "개", tone: "bg-orange" }, { label: "완성한 보고서", value: "2", unit: "개", tone: "bg-surface" }];

export default function Home() { return <div className="mx-auto max-w-[1320px] px-5 py-7 md:px-10 md:py-10"><PageHeader eyebrow="2026. 09. 06 · MONDAY" title="오늘의 현장" description="관찰한 장면을 한눈에 보고 다음 질문을 정리합니다." action={<AddButton>자료 빠르게 남기기</AddButton>} /><section className="mt-7 grid gap-4 md:grid-cols-3">{stats.map((stat) => <div key={stat.label} className={"collage-card " + stat.tone + " p-5"}><p className="text-sm font-semibold">{stat.label}</p><div className="mt-3 flex items-end gap-2"><span className="text-5xl font-extrabold text-orange">{stat.value}</span><span className="mb-1 text-sm font-semibold">{stat.unit}</span></div></div>)}</section><section className="mt-8 grid gap-5 xl:grid-cols-[1.4fr_1fr]"><div className="paper-card p-6"><div className="mb-6 flex items-center justify-between"><div><h2 className="text-xl font-extrabold">성수동 골목 조사</h2><p className="mt-1 text-sm text-secondary">현재 기록 중인 프로젝트</p></div><button aria-label="더 보기" className="rounded-lg p-2 text-ink hover:bg-page"><MoreHorizontal size={20} /></button></div><div className="rounded-xl bg-mint p-5"><p className="text-sm font-semibold">지금 기록하고 있어요</p><p className="mt-1 text-xs text-secondary">마지막 기록 · 10분 전</p><div className="mt-6 flex items-center gap-3"><button className="flex min-h-12 flex-1 items-center justify-center gap-2 rounded-xl bg-ink text-sm font-semibold text-white"><Mic size={18} />음성 메모</button><button className="flex min-h-12 w-12 items-center justify-center rounded-xl bg-surface text-ink" aria-label="사진 추가"><Camera size={18} /></button></div></div></div><div className="paper-card p-6"><div className="flex items-center justify-between"><div><h2 className="text-xl font-extrabold">최근 기록</h2><p className="mt-1 text-sm text-secondary">현장에서 남긴 자료</p></div><Clock3 size={19} /></div><div className="mt-5 space-y-4">{[{ icon: Mic, title: "시장 앞 상인 인터뷰", time: "10분 전", color: "bg-orange" }, { icon: Camera, title: "붉은 벽돌 건물 외관", time: "32분 전", color: "bg-mint" }, { icon: Play, title: "골목 소리 풍경", time: "1시간 전", color: "bg-page" }].map(({ icon: Icon, title, time, color }) => <div className="flex items-center gap-3" key={title}><span className={"flex h-10 w-10 shrink-0 items-center justify-center rounded-lg " + color}><Icon size={16} /></span><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold">{title}</p><p className="mt-0.5 text-xs text-secondary">{time}</p></div></div>)}</div></div></section></div> }
