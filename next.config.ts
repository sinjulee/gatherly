import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  allowedDevOrigins: ["sinjulee-macmini.tail0e1704.ts.net"],
  outputFileTracingRoot: process.cwd(),
};

export default nextConfig;
